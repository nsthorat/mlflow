/**
 * NPMI (Normalized Pointwise Mutual Information) threshold constants
 * used for correlation analysis in traces trends.
 */

export const NPMI_THRESHOLDS = {
  STRONG_CORRELATION: 0.7,
  MODERATE_CORRELATION: 0.3,
  WEAK_CORRELATION: 0.1,
} as const;
